'use client';
import {useState} from 'react';
export default function BeforeAfter({before,after}:{before:string;after:string}){const [value,setValue]=useState(52);return <div className="comparison" style={{'--split':`${value}%`} as React.CSSProperties}><img src={after} alt="فضا پس از بازسازی" loading="lazy"/><div className="before-layer" style={{width:'100%',clipPath:`inset(0 ${100-value}% 0 0)`}}><img src={before} alt="فضا پیش از بازسازی" loading="lazy" style={{width:'100%',objectPosition:'center'}}/></div><span className="comparison-label before-label">پیش از اجرا</span><span className="comparison-label after-label">پس از اجرا</span><input dir="ltr" aria-label="درصد نمایش تصویر پیش از بازسازی" aria-valuetext={`${value} درصد`} type="range" min="0" max="100" value={value} onChange={e=>setValue(+e.target.value)}/><div className="comparison-handle" aria-hidden="true">↔</div></div>}
