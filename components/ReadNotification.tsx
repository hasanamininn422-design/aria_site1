'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function ReadNotification({id}:{id:string}){const router=useRouter(),[pending,setPending]=useState(false),[error,setError]=useState('');return <div><button className="small-button" disabled={pending} onClick={async()=>{setPending(true);try{const r=await fetch('/api/notifications',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});if(!r.ok)throw Error();router.refresh()}catch{setError('ذخیره نشد. دوباره تلاش کنید.')}finally{setPending(false)}}}>{pending?'در حال ذخیره…':'خواندم'}</button><span role="alert">{error}</span></div>}
