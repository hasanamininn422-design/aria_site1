'use client';
import Link from 'next/link';
import {usePathname} from 'next/navigation';
import { useEffect, useState } from 'react';
export default function Header(){ const pathname=usePathname(); const [scrolled,setScrolled]=useState(false); const [open,setOpen]=useState(false); useEffect(()=>{const f=()=>setScrolled(scrollY>32); f(); addEventListener('scroll',f); return()=>removeEventListener('scroll',f)},[]); return <header className={`site-header ${scrolled||pathname!=='/'?'scrolled':''}`}><Link href="/" className="brand" aria-label="آریا کالر، صفحه اصلی"><img src="/media/aria-logo.webp" width="64" height="44" alt="" style={{objectFit:"contain"}}/><span><strong>آریا کالر</strong><small>رنگ، با نگاه معماری</small></span></Link><button className="menu-toggle" aria-expanded={open} aria-controls="main-nav" onClick={()=>setOpen(!open)}><span></span><span></span><span></span><b className="sr-only">منو</b></button><nav id="main-nav" className={open?'open':''}><Link href="/services" onClick={()=>setOpen(false)}>خدمات</Link><Link href="/portfolio" onClick={()=>setOpen(false)}>پروژه‌ها</Link><Link href="/color-selection" onClick={()=>setOpen(false)}>انتخاب رنگ</Link><Link href="/before-after" onClick={()=>setOpen(false)}>قبل و بعد</Link><Link href="/about" onClick={()=>setOpen(false)}>دربارهٔ ما</Link><Link href="/dashboard" onClick={()=>setOpen(false)}>حساب من</Link><Link href="/login" onClick={()=>setOpen(false)}>ورود / ثبت‌نام</Link><Link href="/consultation" className="nav-cta" onClick={()=>setOpen(false)}>مشاورهٔ رنگ <span>↗</span></Link></nav></header> }
