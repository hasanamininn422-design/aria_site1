'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function ProfileForm({name,phone,email}:{name:string;phone:string;email:string}){const router=useRouter(),[pending,setPending]=useState(false),[message,setMessage]=useState('');return <form className="request-form" onSubmit={async e=>{e.preventDefault();setPending(true);setMessage('');const data=Object.fromEntries(new FormData(e.currentTarget));try{const r=await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const result=await r.json();setMessage(result.message);if(r.ok)router.refresh()}catch{setMessage('ارتباط برقرار نشد.')}finally{setPending(false)}}}><label>نام و نام خانوادگی<input name="name" defaultValue={name} minLength={2} maxLength={100} required autoComplete="name"/></label><label>شماره تماس<input name="phone" type="tel" defaultValue={phone} maxLength={30} autoComplete="tel" dir="ltr"/></label><label>ایمیل حساب<input value={email} readOnly dir="ltr"/></label><p>تغییر ایمیل به تأیید مالکیت نشانی جدید نیاز دارد و در این مرحله فعال نیست.</p><button className="btn" disabled={pending}>{pending?'در حال ذخیره…':'ذخیره اطلاعات'}</button><p role="status">{message}</p></form>}
