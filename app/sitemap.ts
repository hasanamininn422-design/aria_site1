import type {MetadataRoute} from 'next';import {services,projects} from '@/lib/content';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';return ['','/services','/portfolio','/about','/consultation','/contact','/color-selection','/before-after','/visit-request','/request-quote',...services.map(s=>`/services/${s.slug}`),...projects.map(p=>`/portfolio/${p.slug}`)].map(path=>({url:`${base}${path}`,lastModified:new Date()}))}
