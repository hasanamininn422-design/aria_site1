import AuthForm from '@/components/AuthForm';export const dynamic='force-dynamic';export const metadata={title:'ساخت حساب کاربری',robots:{index:false,follow:false}};export default function Page(){return <main><section className="page-hero account-hero"><div className="eyebrow">YOUR ARIA COLOR</div><h1>شروع یک همراهی.</h1><p>درخواست‌ها و مسیر تغییر فضای خود را در یک جا دنبال کنید.</p></section><section className="section"><AuthForm mode="register" enabled={Boolean(process.env.DATABASE_URL&&process.env.RATE_LIMIT_SECRET&&process.env.NEXT_PUBLIC_SITE_URL)}/></section></main>}