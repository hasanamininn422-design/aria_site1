import Image from 'next/image';import {notFound} from 'next/navigation';import {projects} from '@/lib/content';import BeforeAfter from '@/components/BeforeAfter';import Link from 'next/link';
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);return {title:p?.title,description:p?.summary,alternates:{canonical:`/portfolio/${slug}`}}}
export default async function Project({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===decodeURIComponent(slug));if(!p)notFound();return <main><section className="page-hero"><div className="eyebrow">{p.category} · نمونهٔ تصویری</div><h1>{p.title}</h1><p>{p.summary}</p></section><section className="section"><Image className="detail-image" src={p.image} alt={p.title} width={1600} height={900}/><div className="split" style={{marginTop:55}}><div><div className="eyebrow">یادداشت پروژه</div><h2>پالت، بافت و نور در یک قاب.</h2></div><div><p>این صفحه یک نمونهٔ ارائه برای ساختار آیندهٔ آرشیو پروژه‌هاست. شرح پروژه، تصاویر و نسبت‌دادن آن به آریا کالر پس از راستی‌آزمایی و ورود به CMS انجام خواهد شد.</p><Link href="/consultation" className="text-link">برای فضای خودتان گفتگو کنید ↗</Link></div></div>{p.before&&p.after&&<div style={{marginTop:75}}><div className="section-head"><div><div className="eyebrow">مقایسه</div><h2>قبل / بعد</h2></div><p>برای جابه‌جایی دستگیره را بکشید. این تصویر صرفاً نمایشی است.</p></div><BeforeAfter before={p.before} after={p.after}/></div>}</section></main>}
