import type {Metadata} from 'next';import Image from 'next/image';import Link from 'next/link';import {notFound} from 'next/navigation';import {services} from '@/lib/content';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=services.find(x=>x.slug===slug);return {title:s?.title||"خدمت",description:s?.description,alternates:{canonical:`/services/${slug}`}}}
export default async function ServiceDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();return <main><section className="page-hero"><div className="eyebrow">{s.eyebrow}</div><h1>{s.title}</h1><p>{s.description}</p></section><section className="section split"><div className="media-frame"><Image src={s.image} alt={s.title} fill sizes="(max-width:800px) 100vw, 50vw"/></div><div><div className="eyebrow">جزئیات اجرا</div><h2>{s.summary}</h2><ol className="number-list">{s.details.map(d=><li key={d}>{d}</li>)}</ol><Link href="/consultation" className="btn">بررسی پروژهٔ شما ↗</Link></div></section><section className="section dark"><div className="section-head"><div><div className="eyebrow">مسیر همکاری</div><h2>آرام، روشن،<br/>مرحله‌به‌مرحله.</h2></div><p>پیش از هر تصمیم نهایی، سطح و شرایط فضا بررسی می‌شود. این صفحه اطلاعات خدمات را معرفی می‌کند؛ قیمت و زمان پس از گفت‌وگو تعیین خواهد شد.</p></div><div className="status-grid"><div className="status-item"><strong>۱ · شناخت</strong><span>نیاز، نور و وضعیت فعلی را می‌شنویم.</span></div><div className="status-item"><strong>۲ · پیشنهاد</strong><span>پالت، پوشش و مسیر اجرا پیشنهاد می‌شود.</span></div><div className="status-item"><strong>۳ · نمونه</strong><span>در صورت نیاز نمونه روی سطح واقعی بررسی می‌شود.</span></div><div className="status-item"><strong>۴ · اجرا</strong><span>کار تمیز و مرحله‌ای تحویل می‌شود.</span></div></div></section></main>}
