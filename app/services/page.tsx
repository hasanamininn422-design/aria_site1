import Link from 'next/link';import Image from 'next/image';import {services} from '@/lib/content';
export const metadata={title:'خدمات'};
export default function Services(){return <main><section className="page-hero"><div className="eyebrow">WHAT WE DO</div><h1>خدماتی برای<br/><em>سطح و نور.</em></h1><p>از انتخاب رنگ تا اجرای نهایی، هر مرحله با توجه به شخصیت فضا و کیفیت ماندگار انجام می‌شود.</p></section><section className="section"><div className="service-grid">{services.map(s=><Link href={`/services/${s.slug}`} className="service-card" key={s.slug}><Image src={s.image} alt={s.title} fill sizes="(max-width:800px) 50vw, 25vw"/><div className="card-content"><div className="eyebrow">{s.eyebrow}</div><h3>{s.title}</h3><p>{s.summary}</p></div></Link>)}</div></section></main>}
