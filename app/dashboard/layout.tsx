import Link from 'next/link';import {redirect} from 'next/navigation';import {currentUser} from '@/lib/auth';import AccountActions from '@/components/AccountActions';export const dynamic='force-dynamic';export const metadata={title:'حساب من',robots:{index:false,follow:false}};export default async function Layout({children}:{children:React.ReactNode}){const user=await currentUser();if(!user)redirect('/login');return <main className="workspace"><aside className="workspace-sidebar"><div className="eyebrow">ARIA COLOR / MY SPACE</div><h2>فضای من</h2><p>{user.profile?.name||user.email}</p><nav aria-label="منوی حساب"><Link href="/dashboard">نمای کلی</Link><Link href="/dashboard/requests">درخواست‌های من</Link><Link href="/dashboard/notifications">اعلان‌ها</Link><Link href="/dashboard/profile">اطلاعات حساب</Link><Link href="/consultation">مشاوره جدید</Link><Link href="/visit-request">درخواست بازدید</Link></nav><AccountActions/></aside><div className="workspace-content">{children}</div></main>}