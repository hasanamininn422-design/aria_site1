import Link from 'next/link';import {redirect} from 'next/navigation';import {currentUser} from '@/lib/auth';import AccountActions from '@/components/AccountActions';export const dynamic='force-dynamic';export const metadata={title:'مدیریت آریا کالر',robots:{index:false,follow:false}};export default async function Layout({children}:{children:React.ReactNode}){const user=await currentUser();if(!user)redirect('/login');if(!['ADMIN','SUPER_ADMIN'].includes(user.role))redirect('/dashboard');return <main className="workspace"><aside className="workspace-sidebar"><div className="eyebrow">ARIA COLOR / MANAGEMENT</div><h2>مدیریت</h2><p>{user.profile?.name||user.email}</p><nav aria-label="منوی مدیریت"><Link href="/admin">نمای کلی</Link><Link href="/admin/requests">تمام درخواست‌ها</Link><Link href="/dashboard/notifications">اعلان‌ها</Link><Link href="/dashboard/profile">اطلاعات حساب</Link><Link href="/dashboard">پنل مشتری</Link></nav><AccountActions/></aside><div className="workspace-content">{children}</div></main>}